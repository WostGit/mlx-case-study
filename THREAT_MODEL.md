# Threat Model

The protected secret is a capability or behavior label. The audit population is finite. The adversary succeeds when a reconstruction criterion accepts its guess.

The theorem applies only when the student artifact is deterministic post-processing of the audited transcript.

If training uses omitted route metadata or campaign metadata, the theorem does not apply.
